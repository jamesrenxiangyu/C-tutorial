#include <iostream>

using namespace std;

int main()
{
    int a, b, sum;
    cout << "Input 10 integers, press enter to finish" << endl;
    cin >> a;
    cin >> b;
    sum = a + b;
    cout << sum << endl;
    return 0;
}
